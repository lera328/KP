# Министерство науки и высшего образования РФ

**Федеральное государственное автономное образовательное учреждение высшего образования «Омский государственный технический университет»**

**Кафедра «Автоматизированные системы обработки информации и управления»**

---

## ОТЧЕТ по производственной практике

**студента Шульгиной Валерии Игоревны, группа ПИН-221**

Пояснительная записка

Шифр работы **От-2068998-43-ПИН-221-51 ПЗ**

Направление **09.03.04**

| | ФИО | Подпись |
|---|---|---|
| Руководитель практики от ОмГТУ, доцент, канд. техн. наук | Е.О. Викулов | |
| Руководитель практической работы студента, доцент, канд. техн. наук | А.А. Зубарев | |
| Студент | В.И. Шульгина | |

**Омск 2025**

---

## Задание на производственную практику

**Период прохождения практики:** 23.06.2025 – 19.07.2025

Студенту ОмГТУ группы **ПИН-221** ФИО **Шульгиной Валерии Игоревне**

**Место прохождения практики:** ООО «СОФТЭНК», договор № 1270/1 от «27» Мая 2025 г.

**Задание руководителя от ОмГТУ:** должность — доцент, канд. техн. наук; ФИО — Викулов Е.О.

1. Изучить прикладные и информационные процессы в подразделениях предприятия.
2. Провести анализ и сформировать данные результатов обследования информационных технологий в рамках будущей ВКР.
3. Сформировать начальные предложения для концепции реализации задач по разработке ПО.
4. Выбрать и обосновать тему учебно-исследовательской работы для ВКР.

Руководитель от ОмГТУ _________________ (подпись)

---

## Задание руководителя практики от предприятия

**Студент:** Шульгина Валерия Игоревна

Разработка модуля Telegram-уведомлений для CRM-системы учреждения дополнительного образования «КиберШкола».

- **Задание 1:** Реализация системы привязки учётной записи пользователя к чату Telegram через deep-link и webhook.
- **Задание 2:** Реализация сервисного слоя автоматических уведомлений по событиям системы и REST API.
- **Задание 3:** Разработка React-компонента управления привязкой и развёртывание с настройкой HTTPS.

---

## Реферат

Отчет 31 с., 2 ч., 9 рис., 1 табл., 4 источника.

ПРОИЗВОДСТВЕННАЯ ПРАКТИКА, TELEGRAM, WEBHOOK, DJANGO, REACT, DOCKER, CRM, HTTPS

Цель практики — закрепление знаний и освоение профессиональных навыков разработчика веб-приложений. В процессе практики разрабатывался модуль Telegram-уведомлений для CRM-системы «КиберШкола» в компании ООО «СОФТЭНК». Реализованы: привязка Telegram-аккаунта через deep-link, автоматическая отправка сообщений по событиям, пользовательский интерфейс и инфраструктура HTTPS. Модуль введён в эксплуатацию на боевом сервере.

---

## Содержание

- ВВЕДЕНИЕ
- Глава 1. Задание руководителя практики от ОмГТУ
  - 1.1 Изучение прикладных и информационных процессов
  - 1.2 Анализ информационных технологий
  - 1.3 Начальные предложения для концепции реализации задач
  - 1.4 Выбор и обоснование темы ВКР
- Глава 2. Задание куратора от ООО «СОФТЭНК»
  - 2.1 Выполнение задания 1
  - 2.2 Выполнение задания 2
  - 2.3 Выполнение задания 3
- ЗАКЛЮЧЕНИЕ
- СПИСОК ИСПОЛЬЗОВАННЫХ ИСТОЧНИКОВ
- ПРИЛОЖЕНИЕ А. Таблица задач по дням

---

## Введение

Производственная практика позволяет углубить знания и навыки, приобретённые в процессе обучения, и закрепить их на практике. Актуальность практики заключается в том, что она даёт возможность освоить новые технологии и применить их в реальных промышленных проектах.

Производственную практику я проходила в компании ООО «СОФТЭНК», специализирующейся на разработке надёжных веб-сервисов и программного обеспечения. В период практики велась работа над модулем Telegram-уведомлений для CRM-системы «КиберШкола» — системы учёта посещаемости занятий, контроля абонементов и взаимодействия преподавателей, родителей и администрации учреждения дополнительного образования.

Цель практики — закрепление знаний, полученных в ходе обучения, и изучение технологий, применяемых в профессиональной деятельности разработчика. В ходе практики ставились следующие задачи:

- изучение прикладных и информационных процессов предприятия;
- анализ применяемых информационных технологий;
- разработка модуля Telegram-уведомлений (бэкенд + фронтенд);
- развёртывание модуля на производственном сервере с HTTPS;
- выбор темы выпускной квалификационной работы.

---

## Глава 1. Задание руководителя практики от ОмГТУ

### 1.1 Изучение прикладных и информационных процессов

Во время прохождения практики в компании ООО «СОФТЭНК» были изучены прикладные и информационные процессы в подразделениях компании. Компания специализируется на разработке корпоративного программного обеспечения и веб-сервисов.

Основные подразделения компании:

**Отдел разработки** — включает фронтенд- и бэкенд-разработчиков. Разработка ведётся по гибким методологиям (Scrum, Kanban).

**Отдел тестирования и качества** — проводит функциональное и регрессионное тестирование, обеспечивая стабильность релизов.

**Отдел поддержки клиентов** — обеспечивает техническое сопровождение продуктов.

Текущий проект — CRM-система «КиберШкола» — автоматизирует основные процессы учреждений дополнительного образования: учёт посещаемости и абонементов, управление расписанием, многоролевой личный кабинет (администратор, преподаватель, ученик, родитель). К моменту начала практики в системе существовал базовый модуль `apps.notifications` с моделями шаблонов и журнала доставки, однако фактической доставки уведомлений до пользователей не происходило.

Информационные процессы компании включают систему контроля версий Git/GitHub, таск-трекер GitHub Issues, Docker Compose для управления средами разработки и эксплуатации.

### 1.2 Анализ информационных технологий

В ходе практики был проведён анализ информационных технологий, применяемых в проекте «КиберШкола».

**Система контроля версий:** Git с хостингом на GitHub. Разработка ведётся в feature-ветках с code review перед слиянием.

**Бэкенд:** фреймворк Django 4 на Python 3.12. REST API реализован через Django REST Framework. СУБД — PostgreSQL 14. Приложение запускается через Gunicorn.

**Фронтенд:** React 18, сборщик Vite, маршрутизация React Router, стилизация Bootstrap 5. Взаимодействие с бэкендом — через REST API с JWT-аутентификацией.

**Инфраструктура:** Docker и Docker Compose контейнеризуют все сервисы (БД, бэкенд, фронтенд/Nginx). TLS-сертификат — Let's Encrypt (certbot). Публичный домен — DuckDNS.

**Telegram Bot API:** используется webhook-режим. Для HTTP-запросов к Bot API применена стандартная библиотека `urllib.request` — без внешних зависимостей.

**Среда разработки:** Visual Studio Code, Windsurf Editor.

Таблица 1 — Используемые технологии

| Слой | Технологии |
|------|------------|
| Бэкенд | Python 3.12, Django 4, Django REST Framework, PostgreSQL 14, Gunicorn |
| Фронтенд | React 18, Vite, React Router, Bootstrap 5 |
| Инфраструктура | Docker, Docker Compose, Nginx, Let's Encrypt, DuckDNS |
| Интеграция | Telegram Bot API (webhook), urllib |
| Контроль версий | Git, GitHub |

### 1.3 Сформировать начальные предложения для концепции реализации задач по разработке программного обеспечения

На основании анализа информационных технологий выбрана тема ВКР: «Разработка CRM-системы для учреждения дополнительного образования». Сформированы следующие концептуальные предложения.

**Многоканальная система уведомлений:** система должна уведомлять пользователей через Telegram-бот и, в перспективе, через push-сообщения в кабинете. Telegram-интеграция обеспечивает бесплатную мгновенную доставку.

**Ролевая модель доступа:** разграничение ролей (администратор, преподаватель, ученик, родитель) позволяет настроить тип уведомлений для каждой роли.

**Журналирование доставки:** каждая попытка отправки логируется со статусом (`SENT`/`FAILED`/`SKIPPED`), получателем и содержимым — для оперативной диагностики.

**Безопасность:** токен привязки одноразовый с TTL 24 часа; webhook защищён секретным ключом в URL; соединение только через HTTPS.

**Контейнеризация:** Docker Compose обеспечивает идентичность окружений. Конфиденциальные параметры хранятся в `.env`-файле вне репозитория.

### 1.4 Выбрать и обосновать тему самостоятельной учебно-исследовательской работы для подготовки выпускной квалификационной работы

На основе полученного опыта выбрана тема ВКР: **«Разработка CRM-системы для учреждения дополнительного образования»**.

**Потребность в продукте:** учреждения дополнительного образования зачастую ведут учёт в таблицах Excel или устаревших системах, что приводит к ошибкам и потере данных. Современная CRM позволит автоматизировать ключевые процессы.

**Анализ аналогов:**

*ALFACRM* — система для учебных центров: расписание, абонементы, посещаемость. Недостаток: высокая стоимость лицензии, ограниченная кастомизация.

*Bitrix24* — универсальная CRM-платформа. Недостаток: избыточный функционал для небольших центров, сложный интерфейс.

**Обоснование:** существующие решения либо слишком дороги, либо не покрывают специфику небольших образовательных центров. Разрабатываемая система ориентирована именно на этот сегмент и предоставляет необходимый и достаточный функционал.

---

## Глава 2. Задание куратора от ООО «СОФТЭНК»

В рамках практики было поручено разработать **модуль Telegram-уведомлений** для CRM-системы «КиберШкола». До начала работы доставка уведомлений до пользователей не осуществлялась вовсе. Работа разделена на три задания.

### 2.1 Выполнение задания 1 — Реализация системы привязки аккаунта Telegram

Для связи учётной записи системы с чатом Telegram реализован поток deep-link привязки. Идентификатор чата (`chat_id`) сохраняется в поле `User.telegram_chat_id`. Привязка выполняется через одноразовый токен — модель `TelegramLinkToken`.

**Поток привязки:**

1. Пользователь нажимает «Подключить Telegram» в кабинете.
2. Фронтенд вызывает `POST /api/notifications/telegram/link/`. Бэкенд создаёт `TelegramLinkToken` и возвращает deep-link вида `https://t.me/<bot>?start=<token>`.
3. Браузер открывает чат с ботом по этой ссылке.
4. Telegram присылает webhook с командой `/start <token>`.
5. Обработчик находит токен, проверяет TTL (24 ч) и статус, сохраняет `chat_id`, помечает токен использованным.
6. Бот отправляет подтверждающее сообщение.

На рисунке 1 представлено сообщение бота после успешной привязки аккаунта.

![Сообщение бота после успешной привязки](docs/screenshots/01_bot_start_success.png)

*Рисунок 1 — Сообщение бота @kiber1One1_bot после привязки аккаунта.*

На рисунке 2 представлена модель `TelegramLinkToken`:

```python
class TelegramLinkToken(models.Model):
    """Одноразовый токен для привязки Telegram-аккаунта пользователя."""

    user = models.ForeignKey(
        settings.AUTH_USER_MODEL,
        on_delete=models.CASCADE,
        related_name="telegram_link_tokens",
    )
    token = models.CharField(max_length=64, unique=True, default=_generate_link_token)
    created_at = models.DateTimeField(auto_now_add=True)
    used_at = models.DateTimeField(null=True, blank=True)

    TTL_HOURS = 24

    def is_expired(self) -> bool:
        return (timezone.now() - self.created_at).total_seconds() > self.TTL_HOURS * 3600

    def is_valid(self) -> bool:
        return self.used_at is None and not self.is_expired()
```

*Рисунок 2 — Модель `TelegramLinkToken` (`backend/apps/notifications/models.py`).*

На рисунке 3 представлен обработчик команды `/start <token>`:

```python
def _handle_start(chat_id: str, token: str, message: dict) -> None:
    if not token:
        send_message(chat_id, "Откройте кабинет и нажмите «Подключить Telegram».")
        return

    link = TelegramLinkToken.objects.filter(token=token).select_related("user").first()
    if not link or not link.is_valid():
        send_message(chat_id, "Ссылка недействительна или устарела.")
        return

    user = link.user
    user.telegram_chat_id = chat_id
    user.save(update_fields=["telegram_chat_id"])

    link.used_at = timezone.now()
    link.save(update_fields=["used_at"])

    full_name = user.get_full_name().strip() or user.username
    send_message(chat_id, f"✅ Аккаунт <b>{full_name}</b> успешно привязан.")
```

*Рисунок 3 — Обработчик команды `/start` (`backend/apps/notifications/telegram.py`).*

### 2.2 Выполнение задания 2 — Сервисный слой уведомлений и REST API

**Сервисный слой** (`services.py`) реализует отправку сообщений по трём бизнес-сценариям:

- `notify_parents_about_absence` — уведомление при пропуске занятия;
- `notify_parents_about_makeup_approval` — при подтверждении отработки;
- `send_low_balance_payment_reminders` — напоминание о низком балансе.

Каждая функция рендерит текст по шаблону из `NotificationTemplate` и логирует попытку в `NotificationEvent` со статусом `SENT`/`FAILED`/`SKIPPED`. На рисунке 4 представлена функция отправки уведомления об отработке:

```python
def notify_parents_about_makeup_approval(makeup_request):
    student = makeup_request.student
    lesson = makeup_request.makeup_lesson
    context = {
        "student_name": student.get_full_name() or student.username,
        "lesson_topic": _resolve_topic(lesson, makeup_request),
        "lesson_starts_at": lesson.starts_at.strftime("%d.%m.%Y %H:%M"),
    }
    message = _render_template(
        NotificationTemplate.EventType.MAKEUP_APPROVED,
        context,
        fallback="✅ Отработка подтверждена\n...",
    )
    for parent_profile in _parent_users_for_student(student.id):
        parent = parent_profile.user
        status, details = _send_telegram_message(parent.telegram_chat_id, message)
        _create_notification_log(EventType.MAKEUP_APPROVED, student, parent,
                                 message, status, details)
    teacher = lesson.teacher if lesson else None
    if teacher and teacher.telegram_chat_id:
        teacher_message = (
            f"📅 Новая отработка в расписании\n"
            f"Ученик: {context['student_name']}\n"
            f"Тема: {context['lesson_topic']}\n"
            f"Дата: {context['lesson_starts_at']}"
        )
        _send_telegram_message(teacher.telegram_chat_id, teacher_message)
```

*Рисунок 4 — Функция `notify_parents_about_makeup_approval` (`backend/apps/notifications/services.py`).*

![Уведомление о подтверждённой отработке](docs/screenshots/02_makeup_notification.png)

*Рисунок 5 — Telegram-уведомление об отработке у родителя и преподавателя.*

**REST API** реализован в пяти эндпоинтах (`/api/notifications/telegram/`):

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `status/` | Статус привязки текущего пользователя |
| POST | `link/` | Создание токена и deep-link |
| POST | `unlink/` | Отвязка (очистка `chat_id`) |
| POST | `test/` | Тестовое сообщение |
| POST | `webhook/<secret>/` | Приём апдейтов от Telegram |

Webhook-эндпоинт проверяет секрет в URL перед обработкой тела запроса. На рисунке 6 представлен его код:

```python
@api_view(["POST"])
@permission_classes([AllowAny])
def telegram_webhook_view(request, secret):
    expected = settings.TELEGRAM_WEBHOOK_SECRET
    if not expected or secret != expected:
        return Response({"ok": False}, status=status.HTTP_403_FORBIDDEN)

    try:
        update = request.data if isinstance(request.data, dict) \
                 else json.loads(request.body or "{}")
    except (ValueError, json.JSONDecodeError):
        update = {}

    try:
        handle_update(update)
    except Exception as exc:
        logger.exception("Telegram webhook handler error: %s", exc)

    return Response({"ok": True})
```

*Рисунок 6 — Webhook-обработчик (`backend/apps/notifications/views.py`).*

![Журнал доставки уведомлений в админке Django](docs/screenshots/04_notification_events.png)

*Рисунок 7 — Журнал `NotificationEvent` в Django-админке.*

### 2.3 Выполнение задания 3

React-компонент `TelegramConnect.jsx` реализует управление привязкой Telegram в личном кабинете. При монтировании запрашивает статус (`GET /telegram/status/`), отображает одно из трёх состояний («Не привязан», «Привязан», «Загрузка») и предоставляет кнопки:

- **Подключить Telegram** — `POST /telegram/link/`, открывает deep-link в новой вкладке.
- **Отвязать** — `POST /telegram/unlink/`, очищает `chat_id`.
- **Тестовое сообщение** — `POST /telegram/test/`, проверяет доставку.

Компонент встроен на главные страницы трёх ролей: `StudentHome`, `TeacherHome`, `ParentChildren`. Стилизация — Bootstrap 5.

На рисунке 8 представлены ключевые обработчики компонента:

```jsx
const handleConnect = async () => {
  setBusy(true);
  try {
    const data = await apiService.createTelegramLink();
    if (data?.deep_link) {
      setLink(data.deep_link);
      window.open(data.deep_link, '_blank', 'noopener');
    }
  } catch (e) {
    setError(e.message || 'Ошибка при создании ссылки');
  } finally {
    setBusy(false);
  }
};

const handleTest = async () => {
  setBusy(true);
  try {
    const res = await apiService.sendTelegramTest();
    setTestResult(res?.ok ? '✅ Сообщение отправлено' : '❌ Не удалось отправить');
  } catch (e) {
    setError(e.message || 'Ошибка отправки');
  } finally {
    setBusy(false);
  }
};
```

![UI-блок Telegram-уведомлений](docs/screenshots/03_ui_block.png)

*Рисунок 8 — Блок «Telegram-уведомления» в личном кабинете.*

**Развёртывание.** Приложение работает в трёх Docker-контейнерах: `db` (PostgreSQL), `backend` (Django + Gunicorn), `frontend` (Nginx + React). Прод-конфигурация вынесена в `docker-compose.prod.yml`. Конфиденциальные параметры (токен бота, секрет вебхука, реквизиты БД) хранятся в `.env`-файле вне репозитория.

Поскольку Telegram требует HTTPS-вебхук с валидным сертификатом, был получен субдомен `kiberone-school.duckdns.org` (DuckDNS), указывающий на IP сервера `50.114.115.101`. Через `certbot --standalone` выпущен сертификат Let's Encrypt. На рисунке 9 представлена конфигурация Nginx:

```nginx
server {
    listen 80;
    server_name kiberone-school.duckdns.org _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    http2 on;
    server_name kiberone-school.duckdns.org;

    ssl_certificate     /etc/letsencrypt/live/kiberone-school.duckdns.org/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kiberone-school.duckdns.org/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

*Рисунок 9 — Конфигурация HTTPS-сервера (`frontend/nginx.conf`).*

После настройки инфраструктуры запущена команда `python manage.py set_telegram_webhook`, которая зарегистрировала webhook-URL у Telegram. С этого момента бот начал принимать апдейты.

**Выявленные проблемы и решения:**

1. *Домен указывал на сторонний прокси* — Telegram получал `Connection timed out`. Решение: переход на DuckDNS с прямым A-записью на IP сервера.
2. *Некорректный `chat_id` в БД* (`@username` вместо числового ID) — исправлено очисткой поля и заполнением только через webhook-привязку.
3. *Кэширование фронтенда* — обновлённый UI не появлялся у пользователей. Решение: `no-cache`-заголовки для `index.html` в Nginx.

---

## Заключение

В ходе производственной практики в компании ООО «СОФТЭНК» был разработан полноценный модуль Telegram-уведомлений для CRM-системы «КиберШкола». Работа охватила полный цикл: проектирование модели данных, реализацию бэкенда, REST API, React-интерфейса и развёртывание на боевом сервере. Модуль введён в эксплуатацию и используется реальными пользователями.

В результате прохождения практики освоены следующие профессиональные навыки:

1. **Бэкенд-разработка:** проектирование моделей Django, реализация REST API на DRF, сервисный слой, management-команды.
2. **Интеграция с внешним API:** Telegram Bot API в webhook-режиме, deep-link поток привязки, HTTP-запросы через `urllib`.
3. **Фронтенд-разработка:** самодостаточный React-компонент, взаимодействие с REST API.
4. **DevOps:** конфигурирование Nginx, выпуск TLS-сертификата Let's Encrypt, Docker Compose.

---

## Список использованных источников

1. Лутц М. Изучаем Python. — М.: O'Reilly Media, 2020. — 1280 c.
2. Роналдо Оливейра. Django 4 в примерах. — М.: ДМК Пресс, 2023. — 740 c.
3. Telegram Bot API. Официальная документация [Электронный ресурс]. — URL: https://core.telegram.org/bots/api (дата обращения: 23.06.2025).
4. Docker Documentation. Overview [Электронный ресурс]. — URL: https://docs.docker.com/guides/docker-overview/ (дата обращения: 23.06.2025).

---

## Приложение А. Таблица задач по дням

| № | Дата | Задача | Результат |
|---|------|--------|-----------|
| 1 | 23.06 | Знакомство с проектом: структура репозитория, стек, существующие Django-приложения | Получено общее представление об архитектуре |
| 2 | 24.06 | Изучение модуля `notifications`: модели `NotificationTemplate`, `NotificationEvent` | Зафиксированы точки расширения |
| 3 | 25.06 | Анализ требований к Telegram-интеграции: поток привязки, типы уведомлений, роли | Составлен план реализации |
| 4 | 26.06 | Проектирование модели `TelegramLinkToken`: поля, TTL, методы `is_valid`, `is_expired` | Схема модели согласована |
| 5 | 27.06 | Реализация `TelegramLinkToken`, миграция, регистрация в `admin.py` | Модель и миграция готовы |
| 6 | 30.06 | Реализация `telegram.py`: `_api_call`, `send_message`, `build_deep_link`, `issue_link_token` | HTTP-слой к Bot API готов |
| 7 | 01.07 | Реализация `handle_update`, обработчик `/start <token>` — поиск токена, сохранение `chat_id` | Привязка через Deep Link работает |
| 8 | 02.07 | Реализация обработчиков `/unlink`, `/help` в `telegram.py` | Команды бота реализованы |
| 9 | 03.07 | REST-эндпоинты: `telegram_status_view`, `telegram_link_view` | Два эндпоинта готовы |
| 10 | 04.07 | REST-эндпоинты: `telegram_unlink_view`, `telegram_test_view`, `telegram_webhook_view` | Все пять эндпоинтов реализованы |
| 11 | 07.07 | Регистрация URL-маршрутов в `notifications/urls.py`, подключение к корневому `urls.py` | API доступно по `/api/notifications/telegram/...` |
| 12 | 08.07 | Переменные окружения `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, `TELEGRAM_WEBHOOK_SECRET` | Конфигурация подключена |
| 13 | 09.07 | Добавление Telegram-отправки в `notify_parents_about_absence` | Родители получают уведомление о пропуске |
| 14 | 10.07 | Добавление уведомления преподавателю в `notify_parents_about_makeup_approval` | Преподаватели видят отработки в Telegram |
| 15 | 11.07 | Django-команда `set_telegram_webhook`: формирование URL, вызов `setWebhook`, `getWebhookInfo` | Команда регистрации вебхука готова |
| 16 | 14.07 | React-компонент `TelegramConnect.jsx`: состояние `linked/unlinked`, кнопки действий | Базовый UI-блок готов |
| 17 | 14.07 | Методы в `api.js`: `getTelegramStatus`, `createTelegramLink`, `unlinkTelegram`, `sendTelegramTest` | API-слой фронтенда реализован |
| 18 | 15.07 | Интеграция `TelegramConnect` в `StudentHome`, `TeacherHome`, `ParentChildren` | Блок отображается у всех ролей |
| 19 | 16.07 | Деплой: `git push`, `docker compose build/up`, применение миграций | Бэкенд поднят в проде |
| 20 | 16.07 | Попытка регистрации webhook: `Connection timed out` — домен указывает на сторонний прокси | Проблема диагностирована |
| 21 | 17.07 | Регистрация `kiberone-school.duckdns.org` через DuckDNS, A-запись на IP сервера | Домен доступен публично |
| 22 | 17.07 | Обновление `nginx.conf`: HTTP→HTTPS редирект, HTTPS-блок, ACME-challenge | Конфигурация nginx обновлена |
| 23 | 17.07 | Обновление `docker-compose.prod.yml`: порт 443, монтирование `/etc/letsencrypt` | Compose-конфигурация готова |
| 24 | 17.07 | Сертификат Let's Encrypt через `certbot --standalone`, пересборка `frontend` | HTTPS работает |
| 25 | 17.07 | Регистрация вебхука `set_telegram_webhook`, проверка `getWebhookInfo` | Webhook принимает апдейты |
| 26 | 18.07 | Очистка некорректных `chat_id`, тест доставки | Привязка и доставка корректны |
| 27 | 18.07 | `AdminUsers.jsx`: секция «Дети» в модальном окне родителя с кликабельными ссылками | Переход к ученику из профиля родителя |
| 28 | 18.07 | Исправление URL изображений в портфолио (относительный путь → абсолютный) | Картинки отображаются корректно |
| 29 | 19.07 | Ревью кода, рефакторинг: вынесен `_resolve_topic` в сервисном слое | Код очищен |
| 30 | 19.07 | Написание отчёта по практике, листинги, таблица задач | Отчёт оформлен |
